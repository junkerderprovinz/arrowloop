# Nothing to keep: minification is off (see build.gradle.kts) because the app is
# three Kotlin files and the 75 MB that matters is a native binary R8 never
# touches. This file exists because the release build type names it.
