# Generated by `expo prebuild`, never edited by hand and never committed. The
# things that WOULD be hand edits live in plugins/ instead, so they survive a
# --clean and apply the same way on a laptop as in CI.
/android
/ios

node_modules/
.expo/
dist/
*.log
/native/jniLibs
